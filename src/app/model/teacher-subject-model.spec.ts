import { TeacherSubjectModel } from './teacher-subject-model';

describe('TeacherSubjectModel', () => {
  it('should create an instance', () => {
    expect(new TeacherSubjectModel()).toBeTruthy();
  });
});
